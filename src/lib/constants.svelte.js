// Constants to avoid magic numbers
export const INTERVALL_DURATION_MS = 1;
export const PREP_TIME_SEC = 600;
export const LONG_PAUSE_FACTOR = 2.5;
export const timerStates = $state({
  sessions: 3,
  workSecs: 1500,
  pauseSecs: 330,
});
